{
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start:server": "json-server --watch db.json --port 4000"
  },
  "devDependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
